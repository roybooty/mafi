export const productRoute = {
  "/api/v1/product": {
    GET: () => {},
  },
  "/api/v1/product/:id": {
    GET: (req) => {},
    POST: (req) => {},
    DELETE: (req) => {},
    UPDATE: (req) => {},
  },
} as const;
